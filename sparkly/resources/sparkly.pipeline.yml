# The main pipeline for sparkly
resources:
  pipelines:
    sparkly_pipeline:
      name: sparkly_pipeline
      catalog: workspace
      schema: sparkly_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
